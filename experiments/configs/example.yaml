# Per-experiment config. Load it in your script with:
#   import yaml; cfg = yaml.safe_load(open("experiments/configs/example.yaml"))
# Keep one file per experiment so runs are reproducible and diffable in git.

name: example
dataset: wine
model:
  type: random_forest
  n_estimators: 200
split:
  test_size: 0.25
  stratify: true
